version: 0.1
component: build
timeoutInSeconds: 6000
runAs: root
shell: bash
env:
  # these are local variables to the build config
  variables:
    key: "value"

  # the value of a vaultVariable is the secret-id (in OCI ID format) stored in the OCI Vault service
  # you can then access the value of that secret in your build_spec.yaml commands
  vaultVariables:
  
  # exportedVariables are made available to use as parameters in sucessor Build Pipeline stages
  # For this Build to run, the Build Pipeline needs to have a BUILDRUN_HASH parameter set
  exportedVariables:
    - BUILDRUN_HASH

steps:
  - type: Command
    name: "Define unique chart version suffix"
    timeoutInSeconds: 40
    command: |
      export BUILDRUN_HASH=$(echo "${OCI_BUILD_RUN_ID}" | rev | cut -c 1-7)
      echo "BUILDRUN_HASH: ${BUILDRUN_HASH}"
  
  - type: Command
    timeoutInSeconds: 1200
    name: "Package and push Helm chart"
    command: |
      cd "${OCI_PRIMARY_SOURCE_DIR:-${OCI_WORKSPACE_DIR}/helm-foggykitchen-hello-world}/helm/helm-foggykitchen-hello-world-chart"
      CHART_VERSION="0.1.0-${BUILDRUN_HASH}"
      echo "Packaging chart version ${CHART_VERSION}"
      helm package . --version "${CHART_VERSION}" --app-version "${BUILDRUN_HASH}"
      helm registry login ${HELM_REPO} -u ${HELM_REPO_USER} -p ${USER_AUTH_TOKEN}
      helm push *.tgz ${HELM_REPO_URL}
